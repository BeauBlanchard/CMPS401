Symbol Table:

# - Wall
* - small blip (food)
@ - big blip (food)
E - empty space
C - pac man spawn
G - ghost spawn

Spacing and alignment of text file is
correct when added to a Visual Studio 
project via add->new item->text file->
utility->Text File (or equivalent).